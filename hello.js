// hello.js

// Declare a global variable clickCount and initialize it to 0
var clickCount = 0;

// Define a function named hello
function hello() {
  // Increment clickCount
  clickCount++;

  // Display a popup alert
  alert("You clicked the button " + clickCount + " times");
}
